#!/usr/bin/env bash
#MISE description="fan out git pull --ff-only + ./bootstrap.sh to the fleet (scripts/update-hosts.sh --help for options)"
set -euo pipefail
root="$(cd "$(dirname "$(readlink -f "$0")")/.." && pwd)"
# shellcheck disable=SC2034  # part of the common task preamble; unused by this task
state="${XDG_STATE_HOME:-$HOME/.local/state}/workstation"
env_has() {
  case ",${MISE_ENV:-}," in *,"$1",*) return 0 ;; esac
  return 1
}
exec "$root/scripts/update-hosts.sh" "$@"
