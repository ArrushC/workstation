# CLAUDE.md

`workstation` is a self-contained dev-environment-provisioning system (Make + chezmoi) for Linux hosts + one Windows host.

**User-facing reference is `README.html`** (open in a browser from the repo root — loads `docs/README/README.css` + `docs/README/README.js`). The static developer console defaults to dark and supports light/system themes, passage search, keyboard navigation, platform quickstart tabs, category filtering with grid/list views, and troubleshooting controls; all documentation remains readable without JavaScript. When in doubt, point users at README.html sections rather than re-explaining them here.

This file is Claude-internal and deliberately kept lean (it loads into context every session). It carries the **rules and tripwires**; the verbose *why*, per-file gotchas, and verification recipes live in `docs/claude/` — pointers in "Claude-internal reference docs" below. When a tripwire fires (you're about to touch a listed area/file), read the matching doc before editing.

## Claude memory routing

Project-scoped memories (user/feedback/project/reference, per the standard `auto memory` taxonomy in the system prompt) live in **`.claude/memory/`** at the repo root — committed, code-reviewable, reproducible after a fresh clone. Write new memory files to `.claude/memory/<slug>.md` and update the index at `.claude/memory/MEMORY.md`. **Do NOT write to the home-dir memory path the system prompt suggests** (`~/.claude/projects/.../memory/`) — that path is intentionally left unsymlinked, so writes there would land outside source control and be invisible to other checkouts and reviewers. Escalate to a host-global memory location ONLY when the user explicitly asks for a memory to apply across all projects (and surface that scope choice in your reply so it's auditable).

## Before you change anything

The repo has a long history of regressions, and the load-bearing rules below were written in response to specific past breakage. Before editing a tricky area:

```bash
git log --oneline -- <path>            # prior fixes/reverts on this file
git log -p -S '<symbol>' -- <path>     # when a symbol was added/removed and why
git log --all --grep='<keyword>'       # search commit subjects (e.g. "crlf", "chmod", "preserve-env")
```

Also skim `CLAUDE_CHANGELOG.md` — the worked-examples archive of past user-facing changes and what README updates each required. If a change you're about to make rhymes with a row in that file, follow the same pattern.

If a fix smells like it could re-break a past pattern (CRLF endings, executable bits, `$(SUDO)` threading, sentinel blocks, parallel-make ordering), `git log` the affected file FIRST.

## Where things are documented

User-facing docs — point users here:

| Topic | See |
|---|---|
| What the repo is, three consumption modes | `README.html` §intro |
| `dev_machine` vs `prod_machine` table, MODE mapping | `README.html` §machines |
| Repo layout tree | `README.html` §layout |
| `hosts.conf` format + `manage-hosts` CLI (bash + ps1) | `README.html` §hosts |
| `bootstrap.sh` flags + 6-step flow | `README.html` §setup-linux |
| `bootstrap.ps1` / Windows + the two terminals (Warp, Windows Terminal) | `README.html` §setup-windows, §setup-warp, §setup-windows-terminal |
| WSL setup | `README.html` §setup-wsl |
| Adding a tool / adding a host walkthrough | `README.html` §adding |
| Daily workflows (`cze`/`cza`/`czd`/`czu`/`czs`, `make`, `update-hosts.sh`) | `README.html` §daily |
| Troubleshooting (31 entries) | `README.html` §troubleshooting |
| Dozzle + Cockpit web admin (dev_machine only) | `README.html` §stack > Web admin card; §troubleshooting; §daily-services |

## Claude-internal reference docs

Deep detail split out of this file to keep it lean. Read the matching doc when its tripwire fires:

| Doc | Read it when | Tripwire below |
|---|---|---|
| `docs/claude/invariants.md` | About to touch an architectural area (Make scope, chezmoi gating, WSL, SSH, terminfo, version pins) — full *why* + failure modes | "Load-bearing invariants" index |
| `docs/claude/file-care.md` | About to edit a specific tracked file — full per-file CRLF/BOM/mode/vendoring/sentinel/parity constraints | "Files Claude should be careful with" summary |
| `docs/claude/verification.md` | Verifying a change — the copy-paste recipe for that subsystem | — |

## Claude Code hooks (edit-time enforcement)

Hooks complement `check-invariants.sh` by moving enforcement to **edit time** and feeding it back to Claude. **Edit any hook → re-run `bash .claude/hooks/test-hooks.sh`** (asserts every decision). All parse hook JSON via `jq`→`python3`→fail-open; they emit compact JSON (PostToolUse `additionalContext`, exit 0; PreToolUse `permissionDecision` deny/ask). They're covered by `check-invariants.sh` (LF+0755 for `.claude/hooks/*.sh`, shellcheck for both sets). Cross-platform via Git-for-Windows bash (same precedent as `notify.sh`; needs `jq` on PATH or it fails open).

- **Repo** (`.claude/hooks/*.sh`, wired in `.claude/settings.json`, this repo only):
  - `post-edit-guard.sh` — PostToolUse; after you edit a run-directly `*.sh` (`makefile/lib/*.sh`, `scripts/*.sh` incl. `scripts/lib/*.sh`), a bare `tasks/*` script, `.githooks`, or a BOM `.ps1`, **auto-repairs** CRLF→LF, the +x/100755 mode, and the `.ps1` BOM, then says re-read.
  - `parity-reminder.sh` — PostToolUse; nudges the sibling on a parity/dual-edit (zshrc↔bashrc, manage-hosts.sh↔.ps1, python-env.sh↔bootstrap.ps1 `$PythonLibs`, the Make-era `versions.mk` pins, `config*.toml` tool pins ↔ `bootstrap.ps1` `$PortableTools`/`MISE_VERSION`, .chezmoiignore target-paths).
  - `memory-routing-guard.sh` — PreToolUse; **denies** writes to `~/.claude/projects/*/memory/` (the base prompt's suggestion), redirecting to `.claude/memory/`.
  - `sync-tool-memory.sh` — PostToolUse; on edits to `config.toml`/`config.*.toml`/`makefile/versions.mk`/`makefile/packages.mk`, regenerates the `TOOLS` block in `chezmoi/private_dot_claude/CLAUDE.md`; nudges to refresh `mise.lock` and run `cza`.
  - `session-context.sh` — SessionStart (session-lifecycle, not edit-time); injects one compact `additionalContext` block of runtime facts: chezmoi deploy state (pending vs `$HOME` in sync), host identity + dev/prod group + EL family, WSL + **static** interop capability (probes `binfmt_misc/WSLInterop` + `command -v powershell.exe`, never spawns a Windows process), and guard-tool readiness (jq/shfmt/gitleaks/shellcheck + pre-commit). Each segment fail-opens independently; `chezmoi` probe is `timeout`-bounded.
  - `session-end-notify.sh` — SessionEnd (session-lifecycle); when the repo is left with uncommitted changes and/or a pending `chezmoi apply` (and the end isn't a `/clear` or `resume`), fires a toast via the global `~/.claude/notify.sh`. The toast is fired **detached** (`setsid`, subshell fallback) so the hook returns instantly — a synchronous toast gets cancelled ("Hook cancelled") during SessionEnd shutdown. Emits no context (SessionEnd can't). `WORKSTATION_NOTIFY` overrides the notifier for the hook test.
- **Global** (`chezmoi/private_dot_claude/hooks/executable_*.sh` → `~/.claude/hooks/`, wired in `modify_private_settings.json`, all dev machines):
  - `secret-guard.sh` — PreToolUse; **denies** edit/read of the age identity (`~/.config/chezmoi/key.txt`), SSH private keys, `*.pem`/`*.key`; **asks** on Bash naming them.
  - `dangerous-command-guard.sh` — PreToolUse; **denies** truly-never-legit Bash (fork bomb, `dd`/`>` raw device, `mkfs`, `chmod -R 777 /`); **asks** on recursive `rm -rf` of `/`|`~`|`$HOME`, `curl|bash`, force-push, recursive `chmod 777`. It matches command **text**, so a command that merely *mentions* a pattern (e.g. a commit message describing it) is screened too — reword or approve the prompt; that mention-matching is why the `rm -rf` tier is *ask*, not *deny*.

## Load-bearing invariants

README documents the *what*; these are the *why* and the failure modes. Each exists because the opposite was tried and broke. **One-line index below — full detail (failure stories, verify commands) in `docs/claude/invariants.md`. Read that file before acting on any entry you're about to touch.**

**Mechanical enforcement:** `scripts/check-invariants.sh` checks the mechanically-checkable subset of the invariants below (version-pin dual/triple-edits, the LF+0755 set, `.ps1` BOMs, unbraced `$name:` drive-lookalike refs in every tracked `.ps1` — PowerShell's parser reads `"$name:"` inside a double-quoted string/here-string as a drive-qualified variable and throws a parse error unless it's a real scope (`$env:`/`$script:`/…) or braced (`${name}:`) — sentinel-block matching, chezmoiignore target-paths, script-flag↔completion parity, bumper-EXCLUDE coverage of every dual-edit pin) and runs shellcheck at warning+, `shfmt -i 2` formatting, and a `gitleaks` committed-secret scan over the first-party shell set (CI installs both from `config.linux.toml` via `jdx/mise-action` so they enforce there too — with `MISE_LOCKFILE=false`, because a non-`$HOME/.config/mise` config root makes `mise install` rewrite the lock sidecar refs; see `docs/claude/file-care.md`). `scripts/check-templates.sh` renders ALL tracked templates for both host groups — rc files, `.chezmoiscripts/*.tmpl`, `dot_config`/dotfile/AppData templates (only `.chezmoi.toml.tmpl` is excluded: config template, needs prompt data) — and syntax-checks the output (zsh/bash/gitconfig/nu/pwsh + yq for YAML + python3 tomllib for TOML; soft-skips missing checkers locally, the lint.yml `templates` job enforces the full set in CI). Run it via `make -C makefile lint MODE=prod` (a root `GNUmakefile` forwards all goals to `makefile/`, so `make lint MODE=prod` works from the repo root as well as via `-C makefile`), install it as a pre-commit hook via `make -C makefile install-hooks`, or let CI (`.github/workflows/lint.yml`) run it. **When you add a new invariant of one of these shapes, add a check there too.** PowerShell scripts are linted by PSScriptAnalyzer (`scripts/check-ps.ps1` / `PSScriptAnalyzerSettings.psd1`) in the same CI workflow; run locally with `make -C makefile ps-lint`. `scripts/check-readme.mjs` drives `README.html` + `docs/README/README.js` under jsdom (CI `readme` job; locally `npm install --no-save --no-package-lock jsdom@30.0.1 && node scripts/check-readme.mjs`; `node_modules/` is gitignored) — search titles without the injected chip count, filters surviving in-page anchor clicks, one navigation per Back — the 2026-09 console redesign shipped with all three broken. A weekly `version-bumps.yml` workflow runs `scripts/bump-versions.sh`, which bumps mise tool pins via `mise outdated --bump` + `mise config set` + refreshed lockfiles and PRs any drifted single-location `versions.mk` pin (the dual/triple-edit pins are reported, not auto-edited; a new dual-edit pin MUST join the script's `EXCLUDE` list, and `check-invariants.sh` asserts this by deriving the pin set from its own `check_version_pins` — the gap that failed version-bumps run #9).

- **`bootstrap.sh` is a thin seed** — no `*_VERSION`/install logic beyond the one exception (`MISE_VERSION`/`MISE_SHA256`, the pinned mise binary it sha256-verifies into `~/.local/bin` before handing off); everything else lives in `config*.toml` or `makefile/`. If it and `scope.mk` disagree on scope, fix `scope.mk`.
- **Tool pins live in `config.toml`/`config.linux.toml`/`config.dev.toml` + the three lock files (`mise.lock`, `mise.linux.lock`, `mise.dev.lock`) + `locks/**`** — this repo root IS mise's global config dir; mise's own lockfile mechanism replaces the old stamp-filename trick. Never hand-edit `mise*.lock` or anything under `locks/**` (regenerate with `mise lock --global` from outside the checkout via the `XDG_CONFIG_HOME` symlink form, then normalize the sidecar paths — see `docs/claude/file-care.md`'s lock-files entry and `scripts/bump-versions.sh`). `makefile/versions.mk` keeps only the five Make-owned pins (claude-cli, Dozzle, Nerd Font, vcpkg, python-env) plus the `ZJSTATUS_ZELLIJ_FLOOR` floor, until PR2. Because the checkout IS `~/.config/mise`, running `mise use -g …`, `mise settings set …`, or a `make tools` after a hand-edited pin all write straight to these TRACKED files — commit or revert them before moving on, never leave the checkout dirty (`update-hosts.sh` pulls `--ff-only`, which fails against local changes).
- **`$(SUDO)` thread** — macros (`TOOL`) use `$(SUDO)` (= `sudo --preserve-env=DEST` on dev, empty on prod), never literal `sudo`. New scope-aware env vars join the `--preserve-env` list.
- **`MODE` has no default** — `scope.mk` errors at parse time if missing; the `dev`/`prod` goals infer it in `makefile/Makefile` BEFORE `include scope.mk` (a recipe-level `$(MAKE) MODE=… provision` re-invocation can never work — the parse-time error fires first; that was the aliases' original, dead-on-arrival design); `bootstrap.sh` rejects `--full`/missing flags.
- **Provisioning is parallel-by-default; phases are recipe lines, NOT prereqs** — top-level make self-assigns `-j$(nproc)` (`MAKELEVEL`-guarded in `makefile/Makefile`, duplicated in root `GNUmakefile`; `-j1` = serial escape hatch). `provision` = three sequential recursive phase lines (packages → `PROVISION_FANOUT` → dotfiles, the latter now including the mise-backed `tools` target); a prereq list CANNOT express phases (prereqs of one target are unordered under `-j`). New provision-phase targets join `PROVISION_FANOUT`. `packages-core→epel→optional` is a real dependency chain. `dotfiles` must stay the UNSYNCED serial tail (chezmoi prompts turn invisible under output-sync); `--output-sync=target` is passed explicitly to the fanout child only — `-O` never appears in a child's parse-time MAKEFLAGS, so it can't be set-and-detected globally.
- **Makefile recipe lines are tab-indented**; macro bodies use `$$` for deferred (rule-fire-time) expansion.
- **Post-install `tasks/verify-tools` gate** — after `mise install`, `scripts/lib/mise-install.sh` (invoked by `make tools`) hands off to `tasks/verify-tools`, which runs `scripts/lib/verify-binary.sh` over every ELF binary `mise bin-paths` exposes, failing the install loudly if one can't run on this host — no silent "installed but won't run" (the original motivation: fastfetch's musl loader + usql's glibc floor). Non-executing: ELF+arch → `ldd` (static→PASS; `not found`→FAIL) → glibc-floor. **Static binaries MUST pass** — don't "tighten" the check to fail non-dynamic binaries; it would false-fail the many musl tools. Symlinked bins (mise's `.mise-bins/` indirection) are followed, not skipped. Unlike the old install-time `&&`-chain, this is a **post-install** gate over everything mise just installed, not a pre-stamp gate per tool — a failure means picking an explicit `github:` `asset_pattern` for that tool in `config*.toml`. Full detail + verify recipe in `docs/claude/invariants.md`.
- **ccstatusline `# CCSTATUSLINE:START/END` sentinel block** in `chezmoi/.chezmoiignore.tmpl` — auto-generated by `scripts/setup-ccstatusline.sh`; hand-edits inside get clobbered.
- **machine-level Claude memory `chezmoi/private_dot_claude/CLAUDE.md` → `~/.claude/CLAUDE.md`** (dev-only via the existing `.claude` chezmoi gate); the `<!-- TOOLS:START/END -->` block is auto-generated by `scripts/gen-tool-memory.sh` from `config*.toml` + `versions.mk` + `packages.mk` (binaries come from mise, dnf lists from `packages.mk`) — never hand-edit inside.
- **`workstation-lsp` skills-dir plugin** (`chezmoi/private_dot_claude/skills/workstation-lsp/`, dev-only via the `.claude` gate) registers the installed LSP servers with Claude Code's `LSP` tool via `dot_lsp.json` (→ `~/.claude/skills/workstation-lsp/.lsp.json`). Auto-loads by presence; `workstation-lsp@skills-dir` in the enforced `enabledPlugins` of `modify_private_settings.json` is the belt-and-suspenders enable. **The plugin installs nothing** — binaries come from `make tools MODE=dev` (mise; `make lsp-servers MODE=dev` only verifies `clangd`). Diagnostics-on/off toggle documented in the plugin's `SKILL.md`, NOT in `.lsp.json`. **`.lsp.json` is strict JSON** — the runtime `LSP` loader requires it (`//` comments break server loading; `claude plugin validate` is JSONC-tolerant but the runtime is not).
- **`JETBRAINSMONO_NERD_VERSION` triple-edit** — `versions.mk` ↔ `lib/font.sh` SHA case ↔ `install-nerd-fonts.ps1` (`.tar.xz` vs `.zip`, different hashes).
- **Windows tool installs are admin-free binary/portable downloads under `%LOCALAPPDATA%\workstation`** (no Chocolatey). `bootstrap.ps1` installs chezmoi via the official `get.chezmoi.io` binary installer (→ `workstation\bin`) and Starship + Helix + Nushell + dnGrep + LogExpert via pinned, sha256-verified portable `.zip`s (→ `workstation\bin` / `workstation\helix` / `workstation\nu` / `workstation\dngrep` / `workstation\logexpert`), all added to the User PATH. **The portable GUI tools (dnGrep, LogExpert) additionally get per-user Start Menu shortcuts** via the opt-in `Shortcut = @{ Target; Description }` key on their `$PortableTools` entries, consumed by the data-driven `Invoke-StartMenuShortcuts` step (`<Name>.lnk` → `<Dest>\<Target>.exe`) — the portable `.zip`s ship none, so Start would otherwise have nothing to launch (this is the portable-tool counterpart to the installer-class apps below, which create their own). It runs every bootstrap **independent of the install stamps** (a deleted shortcut self-heals on re-run) and is **duplicate-proof by fixed filename** (`.Save()` overwrites in place, never adds a second copy); each tool soft-fails if its target exe can't be resolved. **dnGrep additionally gets a seeded `dnGrep.config.xml`** (the `Invoke-DnGrepConfig` step, every bootstrap, **seed-if-absent ONLY** — dnGrep's own Options dialog rewrites the file) redirecting `DataDirectory`/`LogDirectory` to `%APPDATA%\dnGREP` as **expanded absolute paths** (dnGrep doesn't expand `%ENV%`): dnGrep otherwise stores settings next to the writable exe, where the `tree`-layout wipe on a pin bump would destroy them. **The step must also pre-create/heal the redirect-target dirs every run** — dnGrep enumerates `DataDirectory` at startup (`AppTheme.LoadExternalThemes`) and **crashes** on a dangling redirect; it auto-creates only its DEFAULT dir, never a config-file value (bit the first launch, 2026-07-15). Both dnGrep and LogExpert are portable-not-installer-class deliberately — dnGrep ships only machine-scope WiX MSIs (UAC), LogExpert's setup exe is admin-only Inno; and **LogExpert is framework-dependent (needs the hand-installed .NET 10 Desktop Runtime** — the bootstrap installs no runtimes). **Starship/Helix/Nushell/dnGrep/LogExpert pins (version + sha256) live in `bootstrap.ps1`'s `$PortableTools`, NOT `versions.mk`** (Make never runs on Windows — same precedent as `install-nerd-fonts.ps1`); the **Helix pin is a dual-edit with `helix` in `config.linux.toml`** (a mise aqua-registry tool now; Linux + Windows bumped together); dnGrep/LogExpert are Windows-only — no Linux half, no dual-edit (the Nushell precedent). **jq is also a pinned portable tool** — but a bare single-`.exe` release (`Layout = "exe"`, no zip), and its pin dual-edits `jq` in `config.linux.toml` (a mise aqua-registry tool) so the Claude Code hooks can parse JSON with jq on Windows. **OpenCode and Oh My Pi (`omp`) are pinned portable tools too** (zip→`Layout "single"` / bare exe→`Layout "exe"`, both AVX2-requiring Bun binaries) — each pin dual-edits its entry in `config.dev.toml` (`opencode`, `github:can1357/oh-my-pi`, both `os = ["linux"]` so mise never installs them here; they're the Windows halves of the dev-only Linux mise tools). **gh (GitHub CLI) is also a pinned portable tool** (2026-07-17) — dual-edits `gh` in `config.linux.toml` (Windows half of the both-scopes Linux mise aqua-registry tool; verified by `check-invariants.sh`). **DevToys CLI is a pinned portable tool too** (`Layout = "tree"` → `workstation\devtoys-cli` — self-contained single-file exe + REQUIRED sibling `Plugins/` tree, invoked as `devtoys.cli`); its pin dual-edits `github:DevToys-app/DevToys` in `config.dev.toml` (`os = ["linux"]`; the Windows half of the dev-only Linux mise tool). **mise is a pinned portable tool too** (2026-09-16; `Layout = "tree"` into `workstation\mise` with the opt-in `BinSubdir = "bin"` — the only user of that key, `Install-PortableTool` honours it in the stamp check and the PATH entry; a three-way dual-edit — this `$PortableTools` entry, `MISE_VERSION` in `bootstrap.sh`, and `min_version` in `config.toml`); right after chezmoi applies, `Invoke-MiseRuntimes` (step 4b) runs `mise install` straight against `config.toml` + `config.dev.toml` at the root of the checkout — no conf.d, nothing chezmoi-generated or copied — installing node/Go/uv/gopls/the LSP servers/ccstatusline (everything not marked `os = ["linux"]`); uv is one of those mise tools, not a separate portable tool. **Claude Code installs natively via a bespoke `Invoke-InstallClaudeCode` step** — runs the OFFICIAL `claude.ai/install.ps1` (manifest-sha256-verified upstream), skip-if-present, self-updates thereafter (no pin — the rolling model of Linux's `CLAUDE_VERSION := latest`), warn-and-continue on failure, skipped by `-SkipToolInstall`. The Windows Helix runtime is the portable `runtime/` bundled next to `hx.exe` — **no `HELIX_RUNTIME` env var on Windows**, matching Linux now too (the mise-installed aqua archive carries `runtime/` beside `hx` there as well, auto-discovered — `HELIX_RUNTIME` is gone from both scopes). **Git and curl.exe are hard prerequisites** (preflight hard-fails if either is absent; Git the user installs, curl.exe ships with Windows 10 1803+ and is the ONLY HTTP transport: every download and API call in `bootstrap.ps1` and `install-nerd-fonts.ps1` goes through the shared `Invoke-CurlRequest` helper, which downloads to a temp file, checks the native exit code, aborts a stalled transfer via `--speed-limit`/`--speed-time`, echoes curl's stderr into the thrown message, and passes headers via `--header @file` so the PAT never sits in argv); **gh is NOT** — the tool step installs it (see the gh pin sentence above). Zed/VSCode soft-warn; zoxide is not installed. No elevation anywhere.
  **There is also an installer class (`$InstallerTools` in `bootstrap.ps1`, e.g. Obsidian + Zed + DevToys + DBeaver + WinSCP + Beyond Compare)** for apps installed via a silent `.exe` (no portable zip worth pinning): `Install-InstallerTool` resolves the **LATEST** GitHub release (NOT version-pinned — the app self-updates after the seed; so **no `versions.mk`/`$PortableTools`-style pin**), verifies the download against the GitHub API's per-asset sha256 `digest` (hard-fail on mismatch, warn+proceed if absent), runs it silently **per-user** via each tool's `SilentArgs` (Obsidian = NSIS `/S`; Zed = Inno Setup `/VERYSILENT /SUPPRESSMSGBOXES /NORESTART`, `PrivilegesRequired=lowest`; DBeaver = NSIS MultiUser `/S /currentuser`; WinSCP = Inno `/VERYSILENT /SUPPRESSMSGBOXES /NORESTART /CURRENTUSER`), never `/allusers`/machine-wide → no admin, and **adds nothing to PATH** (GUI apps make their own shortcut). Idempotency is by **Uninstall-registry `DisplayName`** via `Test-InstallerPresent` (HKCU + HKLM/WOW6432Node), not a version stamp — a manual uninstall makes the next run reinstall. `-ForceInstaller` forces reinstall; `-SkipToolInstall` skips installer tools too. The class has six OPT-IN per-tool fields (absent = old behavior): DevToys carries `IncludePrerelease` (all its 2.x releases are `prerelease:true`, so `/releases/latest` returns 2023's v1.0.13.0 — the resolver takes the newest non-draft of `/releases` instead) and `UpdateHint` (DevToys does NOT self-update — its in-app check is notification-only; Doctor/CheckForUpdates print the hint instead of "self-updates"); DBeaver carries `TagPrefix` (its git tags are bare — `26.1.2`, no `v` — so the `-CheckForUpdates` tag lookup overrides the default `v` with `""`); WinSCP carries `TagPrefix` too plus the direct-URL pair `UrlTemplate` + `HashManifest` (it publishes NO GitHub release assets — version resolves from its bare git tags via `Get-LatestGitTag` [beta tags dropped by the default filter], the installer downloads from winscp.net's `{VERSION}` URL template [redirects to a SourceForge mirror], and sha256 comes from the official winget manifest for that version: hard-fail on mismatch, warn+proceed when the manifest lags a brand-new release — the missing-digest posture; install is Inno per-user `/CURRENTUSER`, update checks prompt in-app → `UpdateHint`); Beyond Compare carries `WingetVersions` + the same direct-URL pair (Scooter Software has NO GitHub presence at all — `Get-LatestWingetVersion` lists the winget-pkgs manifest directory, whose subdir names ARE the 4-part versions its scootersoftware.com `{VERSION}` URL needs, and sha256 comes from that same manifest: version + hash from ONE authority, so a lagging winget seeds the prior version still-verified; commercial trialware — the seed installs the 30-day trial, the user's license key unlocks it; update checks prompt in-app → `UpdateHint`).
  **And ONE best-effort ELEVATED class (`$ElevatedTools`, today only SSHFS-Win)** — the single sanctioned exception to no-admin: SSHFS-Win depends on the WinFsp **kernel driver**, so both MSIs are machine-scope and UAC is unavoidable. Chain: Uninstall-registry detect (already installed → no UAC, ever) → `winget install SSHFS-Win.SSHFS-Win` (manifest pulls WinFsp.WinFsp; `--force` under `-ForceInstaller`) → digest/`Sha256Pin`-verified direct-MSI fallback via `msiexec -Verb RunAs` ONLY when winget is ABSENT (a winget *failure* is never retried via MSI — the cause would recur and re-pop UAC) → every failure warns-and-continues. **No code path in this class may call `Write-Fail`** (it exits) — a hash mismatch refuses the tool loudly but the bootstrap completes; `-SkipElevated` skips the class. NOT pinned in `versions.mk` (latest-release model like `$InstallerTools`; the `Sha256Pin` for the digest-less 2020 sshfs-win MSI lives only in `bootstrap.ps1` — no dual-edit).
- **Nushell is the default LOCAL Windows shell** (replaces PowerShell, which stays installed for .NET/COM/registry tasks + the WSL2 `notify.sh` hook). The default is set by the Windows Terminal `settings.json` `defaultProfile` — **Windows Terminal is Nushell's first-class home; Warp does NOT support Nushell** (unsupported-shell banner + fallback), which is why Warp's session shell is AlmaLinux-9 WSL zsh and its only nu entry is the degraded `Nushell (compatibility)` Tab Config (a hand-added **Nushell** profile, fixed UUID5 GUID, pointing at the portable `%LOCALAPPDATA%\workstation\nu\nu.exe`). Pinned in `$PortableTools` like the other portable tools (`Layout = "tree"` → `workstation\nu`); **NOT in `versions.mk`** and **no Linux install** (nu isn't a Linux tool here — so no dual-edit). Config is chezmoi-tracked at `AppData/Roaming/nushell/config.nu` (**Windows-only — already covered by the blanket `AppData` Linux-ignore, so NO `.chezmoiignore` entry is added**); the Starship prompt is a **generated** file at `%APPDATA%\nushell\vendor\autoload\starship.nu`, written by `bootstrap.ps1`'s `Invoke-NushellStarship` every run (self-heals, like `Invoke-StartMenuShortcuts`) — never hand-edited, never in `config.nu` (nu can't eval the init at parse time; everything under `vendor/autoload` is auto-sourced). Nushell is **pre-1.0 and churns** (breaking changes every few releases): bump the pin deliberately, upgrade **incrementally** (skipping releases can break `config.nu`), and re-check `config.nu` on every bump — the repo-script flag completions (external completer + `workstation_*_flags` records at the end of `config.nu`) are a known churn surface.
- **TWO managed Windows terminals — Warp (primary) + Windows Terminal (compatibility path).** Neither is pinned in `versions.mk`: both are best-effort evergreen winget seeds that self-update, and both seeds live in `Invoke-ToolInstall` next to each other. **Warp** — `Install-Warp` seeds `winget install --id Warp.Warp --scope user` (Inno `/CURRENTUSER` → no UAC; detect via `Test-InstallerPresent -DisplayName Warp`; winget absent → warn + manual link, never `Write-Fail`). Chezmoi owns three static Warp files (`AppData/Local/warp/Warp/config/{settings.toml,keybindings.yaml}` + `AppData/Roaming/warp/Warp/data/themes/catppuccin-mocha.yaml`) — same **sync-from-host** model as WT's `settings.json` (Warp's Settings panel rewrites `settings.toml` and hot-reloads external edits → `chezmoi re-add` after UI edits). Warp's launch surface is its `+` menu, so `Invoke-WarpTabConfigs` regenerates BOTH the local-shell entries and one SSH+zellij entry per `hosts.conf` row into `%APPDATA%\warp\Warp\data\tab_configs\`, wiping **only** `workstation-*.toml` (that prefix IS the managed namespace — user Tab Configs are untouchable by construction). Warp **cannot** register as Windows' default terminal application (warpdotdev/warp#6261) and **cannot run Nushell**. **Windows Terminal** — `Install-WindowsTerminal` is a best-effort evergreen MSIX seed (winget `Microsoft.WindowsTerminal`, per-user by design, Store-serviced — no `versions.mk` pin; detect via `Get-AppxPackage`/`wt.exe`). Chezmoi owns the whole `settings.json` (Store path under `AppData/Local/Packages/...LocalState/`) as a **static sync-from-host file** (`chezmoi re-add` after Settings-UI edits — WT re-serializes the file; see `docs/claude/file-care.md`). Per-host SSH profiles are NOT in settings.json: `Invoke-WindowsTerminalFragments` regenerates `%LOCALAPPDATA%\Microsoft\Windows Terminal\Fragments\workstation\hosts.json` from `hosts.conf` every bootstrap (deterministic UUID5 GUIDs; wipes **only** `*.json` inside the `workstation` fragment dir — user fragments/profiles are untouchable by construction; WT reads fragments at launch → restart to pick up changes; `disabledProfileSources` keeps WT's 1.24 ssh_config generator from duplicating hosts without the `zellij attach` suffix). WT keeps the Windows default-terminal-application role and stays FULLY configured — nothing about it is degraded by Warp's return.
- **zellij chrome is a custom theme, and its config is silent on failure** — `theme "catppuccin-mocha-mauve"` in `chezmoi/dot_config/zellij/config.kdl` **dual-edits** the block name in `themes/catppuccin-mocha-mauve.kdl` (zellij's *built-in* `catppuccin-mocha` accents green `#a6e3a1`, clashing with the mauve `#cba6f7` that **Warp** (accent + cursor) and fzf use — **not** WT, whose Catppuccin Mocha scheme contains no mauve at all [ANSI purple is pink `#f5c2e7`] and needs no change). Both failure modes here are **silent** — a dangling theme name and a `#` comment line (KDL needs `//`) each make zellij fall back to built-in defaults with no error — so `check_zellij_config` in `scripts/check-invariants.sh` asserts the dual-edit, the comment style, a real `setup --check` parse, that `copy_command` stays **unset** (OSC 52 is the only clipboard path crossing SSH, `ad444df`), and that `web_server false` + `web_sharing "disabled"` stay pinned (the aqua-registry `zellij` asset in `config.linux.toml` is already the web-*capable* build — no override needed). `default_layout` is zellij's **built-in** `default`, deliberately not ours: a custom default layout would replace zellij's built-in swap layouts. Config is read at **session creation** — `zellij kill-session main` before verifying. **The tab bar is zjstatus** (dj95), swapped in by overriding the bare `tab-bar` alias in `config.kdl`'s `plugins {}` block — the built-in default layout references that alias, so no custom layout is needed and the swap layouts survive (verified via `dump-layout` on 0.45.1). It is a mise tool (`github:dj95/zjstatus` in `config.linux.toml`); `make tools` copies the installed `zjstatus.wasm` into zellij's DATA dir via `scripts/lib/zellij-plugin.sh` (`~/.local/share/zellij/plugins/zjstatus.wasm` — it asks `zellij setup --check` for `[PLUGIN DIR]`; WASM-magic gate; never sudo; nothing chezmoi-managed). The alias MUST stay the RELATIVE `file:zjstatus.wasm`: zellij resolves it against `/usr/share/zellij/plugins` then that data dir — **never `~/.config/zellij/plugins`** (the first cut installed there → "ERROR IN PLUGIN" on every session, 2026-09-13; `scripts/test-zellij-plugin.sh` now guards the destination via `check_zellij_plugin_installer`) — and uses the string as the `~/.cache/zellij/permissions.kdl` key, whereas absolute/`~` paths expand per host and re-prompt everywhere (`check_zellij_config` asserts it). The zjstatus version (`config.linux.toml`) is ABI-coupled to zellij: `ZJSTATUS_ZELLIJ_FLOOR` in `versions.mk` records each release's stated floor, `check_zjstatus_zellij_coupling` asserts the `zellij` pin in `config.linux.toml` meets it, and the pin sits in the bumper's EXCLUDE — bump both by hand. First load per host is an interactive permission grant (README §daily-zellij). **Judge a plugin load by the log** (`/tmp/zellij-<uid>/zellij-log/zellij.log`: `Loaded plugin 'zjstatus.wasm'` vs `No such file`) — `zellij action dump-layout` echoes the location string whether or not the file was found. The built-in status-bar stays: it renders THIS config's key hints; zjstatus-hints shows stock ones. `layouts/{dev,ops}.kdl` carry the verbatim `--dump-swap-layout default` block (a layout file replaces the built-in swap layouts) with bare bar aliases — re-dump on a zellij bump. `default_mode "locked"` was evaluated and DEFERRED (zellij's own ALT defaults die in locked mode; ALT+F/ALT+D collide with Helix insert mode) — see `docs/superpowers/specs/2026-09-13-zellij-zjstatus-design.md`. Detail in `docs/claude/file-care.md`.
- **Tools are mise-managed via `make tools` (BOTH scopes, USER-LEVEL — never `$(SUDO)` except the one-time legacy sweep).** Every tool in the toolbelt — fzf/zellij/helix/rust-analyzer/…, node/Go/uv/gopls/the language servers, ccstatusline, python — is declared in `config.toml` (cross-platform: uv, python), `config.linux.toml` (the Linux toolbelt, both scopes), and `config.dev.toml` (dev tools on both OSes; Linux-only dev tools carry `os = ["linux"]`). `MISE_ENV` (computed by `scripts/lib/mise-env.sh <dev|prod>`, exported by the rc files / a Windows User var / `~/.config/environment.d/10-mise.conf` for systemd user units, plus a direct unit-level `Environment=MISE_ENV=...` on `pueued.service` itself — systemd user units start with no shell env, so the shim can't resolve `pueued` without it) picks which files load: `linux` (prod) · `linux,dev,host,wsl` · `linux,dev,host,native` · `windows,dev`. `make tools` runs `scripts/lib/mise-install.sh` (`mise install`, force-reinstalling `node` only when its declaration changed while it was already installed — via a cksum marker under `~/.local/state/workstation/`, so the npm postinstall re-runs the language-server install — then `mise prune`), `tasks/verify-tools` (the post-install capability gate), and `tasks/migrate-legacy` (one-time sweep of the pre-mise `/usr/local/bin`/`~/.local/bin` binaries — sudo only for `/usr/local` on dev, skipped entirely with no sudo), plus a copy of `zjstatus.wasm` into zellij's data dir. `python` **IS declared** in `config.toml` (a three-way pin with `PYTHON_VERSION` in `versions.mk` and `$PythonEnvVersion` in `bootstrap.ps1`) — it exists so `uv sync --frozen --python <this>` can solve the dependency-locked `pypi:`/`pipx:` tools (basedpyright, glances, asciinema, harlequin); it shadows `python3` in mise-activated shells, but the system interpreter stays at `/usr/bin/python3` (sudo and system services are unaffected). Non-interactive shells see the tools through `~/.local/share/mise/shims` (`dot_zshenv.tmpl`; `dot_bashrc.tmpl` ABOVE its interactive guard — parity pair), interactive ones through `mise activate`; `lsp-servers` is now a thin dev alias (+ clangd verify only). **Windows half:** the checkout at `%USERPROFILE%\.config\mise` IS mise's config dir there too; `MISE_ENV` is persisted as a User environment variable; `Invoke-MiseRuntimes` in `bootstrap.ps1` (right after chezmoi applies) runs `mise install` directly against `config.toml` + `config.dev.toml` at the checkout root — no conf.d, nothing chezmoi-generated or copied, and `config.linux.toml` never loads (no `linux` token in the Windows `MISE_ENV`).
- **`python-env` is a BOTH-SCOPES bespoke Make target, USER-LEVEL (never `$(SUDO)`)** — uv (a mise tool now, in `config.toml`; the recipe prepends the shims dir) builds a pinned CPython + one venv (`~/.local/share/workstation-python`) with the ad-hoc scripting libs; launchers `wpy`/`textual`/`typer` in `~/.local/bin` (system python never shadowed). This is a *different* Python from the `python` mise itself declares in `config.toml` (that one exists only to solve dependency-locked `pypi:`/`pipx:` tool installs) — same `PYTHON_VERSION` pin by coincidence of the three-way dual-edit, not the same interpreter instance. Stamp bakes `PYTHON_VERSION` + a cksum of `lib/python-env.sh` (libs track latest — upgrade = `make python-env-rebuild`, a serialized `clean-python-env`→`python-env` wrapper; the combined goal list races under -j$(nproc)). Windows half: uv is mise-installed (`Invoke-PythonEnv` resolves it via `mise which uv` after `Invoke-MiseRuntimes`) + `Invoke-PythonEnv` (`$PythonEnvVersion`/`$PythonLibs`).
- **Docker is dev-only via `docker-engine`** — bespoke target, skipped on WSL (Docker Desktop owns the engine there); Docker CE via dnf, no `versions.mk` pin (dnf-versioned, like cockpit); pre-existing docker on PATH is left untouched; `dozzle-service`'s stamp order-only-deps on its stamp *file*.
- **C/C++ toolbelt has three install shapes** (full *why* in `docs/claude/invariants.md`): (1) the bulk is **dnf, dev-only best-effort** in `LINUX_OPTIONAL_PACKAGES` (`gcc-c++`/`clang`/`clang-tools-extra` [clangd+clang-tidy+clang-format]/`llvm`/`lldb`/`valgrind`/`cppcheck`/`ltrace`/`libasan`/`libubsan`/`libtsan`/`cmake`/`meson`/`ninja-build`/`heaptrack`/`bear` [compile_commands.json for clangd]/`ccache`; `gdb`/`perf`/`strace` were already listed; `gdb-gdbserver` — the separate EL subpackage providing the remote-debug stub `gdbserver` (`gdb` doesn't bundle it) — sits next to `gdb`). EPEL/CRB-backed, so `packages-epel` auto-enables EPEL **and** CRB on RHEL-family hosts (EPEL on EL9 needs CRB or `meson`/`ninja-build` [in CRB] + `heaptrack`/`bear` [CRB deps] fail to resolve); the per-pkg `|| true` is the final fail-soft. The `ninja` binary is `ninja-build` on RHEL. (2) **`nnd`** debugger is a both-scopes mise tool (`github:al13n321/nnd` = `"0.80"` in `config.linux.toml`, bare `nnd` asset_pattern — no os/arch tokens, so the aqua registry can't auto-pick it either). (3) **`pwndbg`** is a dev-only mise tool (`github:pwndbg/pwndbg` in `config.dev.toml`, `os = ["linux"]`, `bin_path = "pwndbg/bin"` into the self-contained portable tarball) → a standalone `pwndbg` command that does **NOT** touch `~/.gdbinit`. **`vcpkg`** is still a dev-only bespoke Make target joining `provision` (MODE=dev): `lib/vcpkg.sh` git-clones+bootstraps into `VCPKG_ROOT_DIR` and symlinks the binary into `$(DEST)`. **GEF** is the plain-`gdb` front-end: vendored `chezmoi/dot_config/gdb/gef.py` **pinned to release `2024.06`** (the last GEF supporting Python 3.6–3.9 — EL9 gdb has Python 3.9; newer GEF needs 3.10 and SyntaxErrors on load) + tracked `chezmoi/dot_gdbinit.tmpl`, dev-gated + Windows-ignored in `.chezmoiignore.tmpl` (TARGET paths `.gdbinit`, `.config/gdb`). pwndbg (its own command) and GEF (`gdb`) coexist with no collision; `gdb -nx` = vanilla.
- **`~/.claude/settings.json` is a chezmoi merge-on-apply (`modify_private_settings.json`), NOT a whole-file template** — the source is a `chezmoi:modify-template` (it MUST NOT have a `.tmpl` extension, or chezmoi executes it as a script). Each apply deep-merges three layers, later winning: **seed defaults** (`model`→`opus[1m]`, `effortLevel`, `theme`, `editorMode`, `tui`, `verbose`) set only when absent → **the live file** (`.chezmoi.stdin`) → **enforced keys** (`statusLine`, `hooks`, `enabledPlugins`, `skipAutoPermissionPrompt`, `skipWorkflowUsageWarning`, `remoteControlAtStartup`, `agentPushNotifEnabled`, `inputNeededNotifEnabled`) which always win. So Claude Code's in-app changes to seed keys survive `cza`, while enforced infra/safety keys stay consistent. `setup-ccstatusline.sh` still must never write settings.json (the enforced `statusLine` overrides it anyway). To change an enforced key cross-host, edit the enforced block in `modify_private_settings.json`.
- **chezmoi dev/prod gating via the `group` data field** — `.chezmoi.toml.tmpl` sets `group` from `WORKSTATION_GROUP`; `.chezmoiignore.tmpl` consumes `.group` to skip dev-only paths on prod (as TARGET paths `.claude` + `.config/ccstatusline` — see the target-path tripwire below). New dev-only paths go inside the existing `{{ if and (hasKey . "group") (ne .group "dev_machine") }}` block. Re-run `chezmoi init` to migrate legacy hosts.
- **`.chezmoiignore.tmpl` patterns are TARGET paths, NOT source-state names** — chezmoi matches `.chezmoiignore` against the destination path (`.bashrc`, `.config/zsh`, `.claude`, `.config/ccstatusline`), so a `dot_*`/`private_dot_*`/`*.tmpl` pattern is a **silent no-op** that ignores nothing and deploys the file anyway. This bit every entry once (Linux hosts leaked only a then-tracked terminal-config dir this repo no longer ships; Windows got the entire Linux dotfile set because its chezmoi rarely ran). All branches — both OS blocks, the dev/prod block, and the `setup-ccstatusline.sh`-generated per-host opt-out stanza — must use target paths. Verify after editing: `chezmoi ignored` (Linux) and the Windows-native `chezmoi ignored --source <repo>` must list the intended targets (an empty list = the bug is back).
- **`</dev/tty` redirect in chezmoi init** — load-bearing under `curl | bash` (else `promptStringOnce` reads EOF). First-run `init` is `bootstrap.sh`'s job; `dotfiles.mk` only `chezmoi update`s.
- **WSL detection centralized in `is_wsl()`** in `bootstrap.sh` — extend the helper, don't fork detection.
- **chezmoi source dir is `chezmoi/`** via `.chezmoiroot` (one-line file at root) — don't delete/edit it, or all `dot_*` paths break.
- **chezmoi naming conventions** — `dot_X`→`~/.X`; `dot_config/X`→`~/.config/X`; `private_X`→0600/0700; `.tmpl`→Go templates; `AppData/`,`Documents/` Windows-only. Skills-dir plugin files follow the same convention: `dot_lsp.json`→`~/.claude/skills/<plugin>/.lsp.json`, `dot_claude-plugin/plugin.json`→`~/.claude/skills/<plugin>/.claude-plugin/plugin.json`.
- **age encryption is dormant-by-default + the identity is out-of-band** — `.chezmoi.toml.tmpl` emits the `encryption="age"`/`[age]` block only when `WORKSTATION_AGE_RECIPIENT` is set at `chezmoi init`; the private identity lives at `~/.config/chezmoi/key.txt` and is **NEVER** committed (the recipient public key is fine to commit). `encrypted_*.age` source files decrypt only where the identity is present; `bootstrap.sh`/`bootstrap.ps1` warn (soft) if the recipient is set but the key is missing.
- **`.chezmoiscripts/` are NOT covered by `.chezmoiignore`** — OS-gate via the script body's `{{ if eq .chezmoi.os "linux" }}…{{ end }}`, not ignore patterns.
- **Only two valid host groups** — `dev_machine`/`prod_machine`; both manage-hosts scripts reject others. A third needs `scope.mk` AND `update-hosts.sh` changes.
- **`configs/` is sudo-installed to `/etc/`, NOT chezmoi-managed** — deployed by paired make targets (`dozzle-service`, `cockpit-service`, `rsyslog-service` for `/etc/rsyslog.d/30-workstation.conf`, and `wsl-config` for `/etc/wsl.conf`). Chezmoi owns `$HOME` only. `rsyslog-service` is `MODE=dev`-only + skip-on-WSL (like cockpit/dozzle/docker), content-hash stamped like `wsl-config` (no version to bake — dnf-versioned); it sanity-checks `rpm -q rsyslog` (which lives in `LINUX_OPTIONAL_PACKAGES`) rather than installing the package, then `systemctl enable`+`restart rsyslog` to apply the drop-in (routes the `local0..local7` facilities → `/var/log/workstation.log`). `wsl-config` is `IS_WSL`+`HAS_SUDO`-gated (no-op off WSL / on sudo-less prod), content-hash stamped (no version to bake), joins the base `provision` line, and now auto-applies the `appendWindowsPath=false` flip (was manual per-host — see `project-wsl-appendwindowspath-false.md`). Its Windows counterpart is the chezmoi-managed global `%USERPROFILE%\.wslconfig` (`chezmoi/dot_wslconfig`, ignored on Linux so it deploys only on Windows; `run_onchange` prints a `wsl --shutdown` reminder on change). Full detail in `docs/claude/file-care.md`.
- **SSH multiplexing is gated out on Windows** — the `ControlMaster`/`ControlPersist`/`ControlPath` block in `private_config.tmpl` sits under `{{ if ne .chezmoi.os "windows" }}`. Windows OpenSSH can't multiplex (breaks every ssh incl. git). Linux/macOS keep it.
- **zsh interactive plugin load order** — in `dot_zshrc.tmpl`: fzf-tab after `compinit` → zsh-autosuggestions → zsh-syntax-highlighting → **zsh-history-substring-search LAST** (it must come *after* syntax-highlighting, per upstream; then ↑/↓ are bound to its widgets). syntax-highlighting must still see all earlier widgets (incl. shift-select). zsh-you-should-use is a preexec hook (order-loose). Reorder → highlighting/suggestions/history-nav silently break. fzf-tab needs `use-fzf-default-opts yes` to inherit the Catppuccin `FZF_DEFAULT_OPTS`. Keymap: `Ctrl-R`=atuin (wired with `--disable-up-arrow` so ↑/↓ stay free), ↑/↓=substring-search, →=autosuggest, `Tab`=fzf-tab. All five plugins are zsh-only — `dot_bashrc.tmpl` carries PARITY NOTEs (readline `history-search-*` on ↑/↓ as the substring-search analog; atuin + you-should-use have no bash counterpart). atuin/broot(`br`)/yazi(`y`) are wired tools, not plugins; broot+yazi in both shells, atuin zsh-only (bash needs bash-preexec). `~/.zshenv` (new, Linux-only, ignored on Windows) carries only two guarded PATH prepends — `~/.local/bin` (preserving what the host's pre-chezmoi `.zshenv` provided) and the mise shims — keep it tiny, it runs for every zsh.

## Conventions and rules of thumb

- **Don't fixed-width-pad `hosts.conf`** — manage-hosts re-pads on every save.
- **`scripts/manage-hosts.sh` and `scripts/manage-hosts.ps1` are a parity pair.** Every user-visible capability (flags, menu options, prompts, defaults, post-add flow, output glyphs) must exist in both. Change them in the same commit. Drift silently breaks Linux/Windows reproducibility.
- **All tool installs are mise pins in `config.toml`/`config.linux.toml`/`config.dev.toml`** — never re-introduce per-tool install logic in `bootstrap.sh`, scripts, `makefile/`, or a new playbook.
- **Per-machine overrides go in `~/.zshrc.local`** (Linux, sourced from `dot_zshrc.tmpl`; `~/.bashrc.local` still works if you've started an interactive bash) / `Microsoft.PowerShell_profile.local.ps1` (Windows) / `~/.ssh/config.local` (both). Un-tracked, sourced/included last.
- **After editing any file under `makefile/lib/` or `scripts/`** (shell side): verify with `file <path>` (must NOT say "with CRLF line terminators") and `git ls-files --stage <path>` (must show `100755` for the lib scripts). Repairs: `sed -i 's/\r$//' <path>` and `git update-index --chmod=+x <path>`.
- **After editing `scripts/manage-hosts.ps1` or `bootstrap.ps1`**: verify they retain UTF-8 BOM. PowerShell 5.1 mis-decodes UTF-8 glyphs (`✓`, `✗`, `─`) without one and fails to parse. Restore with `[System.IO.File]::WriteAllText($path, ..., [System.Text.UTF8Encoding]::new($true))`.
- **When changing user-facing surface, update `README.html` (and `docs/README/README.css` / `docs/README/README.js` if needed) in the same commit, then append a row to `CLAUDE_CHANGELOG.md`.** Decision test: "Would a user reading only `README.html` still be able to operate this repo after my change?" If no, README needs an update.

What does NOT need a README update: internal Makefile refactors that don't change CLI overrides or file locations; comment edits / formatting / variable renames invisible outside `makefile/`; bumping a tool version (lives in `config*.toml`, or in `versions.mk` for the five Make-owned pins); internal shell-script refactors; edits to Claude-internal files (this `CLAUDE.md`, `docs/claude/*`, `.claude/`).

## Files Claude should be careful with

Per-file gotchas live in **`docs/claude/file-care.md`** — read a file's entry before editing it. The cross-cutting risk categories (the tripwires):

- **LF-only + mode 100755 in git:** `makefile/lib/*.sh`, `scripts/*.sh` (incl. `scripts/lib/*.sh` — `mise-env.sh`, `mise-install.sh`, `verify-binary.sh`, `zellij-plugin.sh`), `tasks/*` (`verify-tools`, `migrate-legacy`), `.claude/hooks/*.sh`. CRLF or mode 100644 breaks fresh clones (`sudo: archive.sh: command not found`). Repair one-liners in Conventions above. **First-party shell edits must also be `shfmt -i 2`-clean and pass a `gitleaks` scan** — `check-invariants.sh` enforces both (pre-commit + CI); `make -C makefile fmt MODE=prod` auto-formats. `shfmt -w` preserves mode, but re-verify LF+0755 after a bulk format.
- **UTF-8 with BOM (PowerShell 5.1):** `scripts/manage-hosts.ps1`, `bootstrap.ps1`, `scripts/install-nerd-fonts.ps1`. Restore one-liner in Conventions above.
- **Vendored — don't hand-edit; re-download at the pinned tag + refresh sha256:** `chezmoi/dot_config/zsh/plugins/zsh-shift-select.zsh`, `chezmoi/dot_config/zsh/completions/_cht.sh` (rolling — no upstream tag; bump by snapshot date + sha256, `#compdef cht.sh` stays line 1). Also the five pinned zsh plugin dirs `chezmoi/dot_config/zsh/plugins/{zsh-autosuggestions,zsh-syntax-highlighting,fzf-tab,zsh-history-substring-search,zsh-you-should-use}/` — verbatim upstream, provenance in each dir's chezmoi-ignored `.vendor` sidecar; bump = re-download the pinned tag + refresh sha256 in `.vendor`. Also `chezmoi/dot_local/bin/executable_batpipe` — vendored `eth-p/bat-extras` batpipe (the `less` `LESSOPEN` preprocessor for bat-highlighted paging), single self-contained script that **carries a documented 2-line local patch** (parent-detection `awk` `printf $i" "`→`printf "%s ", $i`; upstream bug, still in master — re-apply on bump); provenance + patch in its `dot_local/bin/.vendor` sidecar; LF-only, deploys 0755, no `versions.mk` entry; bump = re-download the release, re-apply the patch, refresh `.vendor`. Also `chezmoi/dot_config/gdb/gef.py` — vendored **GEF** (GDB Enhanced Features), single self-contained script sourced by the tracked `dot_gdbinit.tmpl` so plain `gdb` loads it; **tag-pinned to release `2024.06`** (sha256 in its `.vendor` sidecar). **Load-bearing Python floor:** 2024.06 is the LAST GEF supporting Python 3.6–3.9; EL9 gdb embeds Python 3.9, and GEF `2025.01`+ requires 3.10 (uses `match` syntax → SyntaxError at load → traceback on every `gdb` start). **DO NOT bump past 2024.06 while the fleet is EL9** (constraint + the `gdb -ex 'source gef.py' -ex 'gef help'` verify recipe live in `.vendor`). pwndbg is immune (its portable build bundles a newer Python). Deploys 0644 to `~/.config/gdb/gef.py`, dev+Linux only (gated in `.chezmoiignore.tmpl`), no `versions.mk` entry, no local patch (verbatim upstream).
- **Sentinel blocks (auto-generated; never edit inside):** `chezmoi/.chezmoiignore.tmpl` (`# CCSTATUSLINE:START/END`), `chezmoi/private_dot_claude/CLAUDE.md` (`<!-- TOOLS:START/END -->`).
- **Parity pairs (change both in the same commit):** `dot_zshrc.tmpl` ↔ `dot_bashrc.tmpl`; `manage-hosts.sh` ↔ `manage-hosts.ps1`; `AppData/Roaming/nushell/config.nu.tmpl` ↔ `Documents/PowerShell/Microsoft.PowerShell_profile.ps1.tmpl` (the `cz*`/`g*` aliases — keep both Windows shells in sync); `makefile/lib/python-env.sh` `PY_LIBS` ↔ `bootstrap.ps1` `$PythonLibs` + `Invoke-PythonEnv` (verified by `check-invariants.sh`); `Invoke-CurlRequest` in `bootstrap.ps1` ↔ `scripts/install-nerd-fonts.ps1` (byte-identical copies — bootstrap runs from memory before the repo exists and the font script must run standalone; verified by `check-invariants.sh` `check_curl_helper_parity` and again by `scripts/test-curl.ps1` in the `windows-http` CI job); `dot_zshenv.tmpl` ↔ the shims block above the guard in `dot_bashrc.tmpl`; the `MISE_ENV` template expression in `dot_zshenv.tmpl` ↔ `chezmoi/dot_config/environment.d/10-mise.conf.tmpl` ↔ the `Environment=MISE_ENV=...` line in `chezmoi/dot_config/systemd/user/pueued.service.tmpl`.
- **Version-pin dual/triple-edits:** `JETBRAINSMONO_NERD_VERSION` (`versions.mk` + `lib/font.sh` + `install-nerd-fonts.ps1`); `VCPKG_ROOT` rc literal (zsh+bash, dev-gated) ↔ `VCPKG_ROOT_DIR` in `makefile/Makefile` (both verified by `check-invariants.sh`); **MISE three-way** (`MISE_VERSION` in `bootstrap.sh` ↔ the mise `$PortableTools` entry in `bootstrap.ps1` ↔ `min_version` in `config.toml`); **PYTHON three-way** (`PYTHON_VERSION` in `versions.mk` ↔ `tools.python` in `config.toml` ↔ `$PythonEnvVersion` in `bootstrap.ps1`) — both verified by `check-invariants.sh`; `jq`/`gh`/`helix`/`opencode`/`omp`/DevToys CLI pins are now `config.linux.toml`/`config.dev.toml` ↔ the matching `bootstrap.ps1` `$PortableTools` entry (verified by `check-invariants.sh`); zellij `theme "<name>"` (`dot_config/zellij/config.kdl` ↔ the block name in `dot_config/zellij/themes/*.kdl` — verified by `check-invariants.sh`); zjstatus version (`config.linux.toml`) ↔ `ZJSTATUS_ZELLIJ_FLOOR` (`versions.mk`, bumper-EXCLUDEd; `check-invariants.sh` asserts the pinned `zellij` version in `config.linux.toml` ≥ floor); TypeScript major-version coupling (the `typescript@`/`typescript-language-server@` literals in node's postinstall string, `config.dev.toml`; bumper-EXCLUDEd; `check-invariants.sh` asserts TS major ≤ 5 — TS 7 has no tsserver.js); **script-flag↔completion parity** — `bootstrap.sh`/`manage-hosts.sh`/`update-hosts.sh` ↔ `chezmoi/dot_config/zsh/completions/_*` + `chezmoi/dot_config/bash/completions.bash`, and `bootstrap.ps1`/`manage-hosts.ps1` ↔ the `workstation_*_flags` records in `config.nu.tmpl` (all five pairs verified by `check-invariants.sh`; add a flag → update every surface, or deliberately exclude it in the check like `--full`/`--checkforupdates`).
- **Don't hand-edit (regenerated / placeholder-substituted):** `hosts.conf` (manage-hosts re-pads/sorts), `.chezmoiroot` (one line `chezmoi`), `configs/dozzle/dozzle.env` (`@DOZZLE_VERSION@` is sed-substituted), any `/etc/`-deployed `configs/*` copy (overwritten by its `<tool>-service` target), `mise.lock`/`mise.linux.lock`/`mise.dev.lock`/anything under `locks/**` (mise-generated — regenerate with `mise lock --global`, never hand-edit).
- **Single-source-of-truth files:** `config.toml`/`config.linux.toml`/`config.dev.toml` (every tool pin), `scripts/lib/mise-env.sh` (the `MISE_ENV` token-set logic), `makefile/scope.mk` (MODE→DEST/SUDO/…), `makefile/versions.mk` (the few pins Make still owns, post-mise), `modify_private_settings.json` (the `~/.claude/settings.json` merge-template — enforced keys forced every apply, seed keys set-if-absent; Notification hooks → `~/.claude/notify.sh`), `scripts/gen-tool-memory.sh` (the `<!-- TOOLS:START/END -->` block generator for the machine-level Claude memory file, reading `config*.toml`).

## Quick verification

Recipes per subsystem are in **`docs/claude/verification.md`** — copy-paste the one matching your change (Make scope/dry-runs, sandbox install, hosts/bootstrap, chezmoi diff, SSH keepalive/mux, Windows Terminal/WSL, services, zellij, fonts, tldr, MANPAGER). Run the relevant check and confirm the stated output before claiming done.
