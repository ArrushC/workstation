# Memory index

- [Use PR-review workflow](feedback_use_pr_review_workflow.md) — land changes via feature branch + PR; the user merges. Claude can't push or merge to main (harness blocks it). Supersedes the old "always work on main" preference as of 2026-06-15.
- [Always push after commit](feedback_always_push_after_commit.md) — standing instruction: every commit gets pushed without asking, overriding the default guardrail.
- [Commit .claude/ changes routinely](feedback_commit_claude_dir_routinely.md) — .claude/{settings.json, settings.local.json, memory/*} are regular tracked content. Stop treating their modifications as session-drift to defer.
- [WezTerm: no wezterm.gui.* calls at config-load](project_wezterm_no_gui_calls_at_config_load.md) — calling screens()/enumerate_gpus() at config-parse time DEADLOCKS WezTerm (froze it in #39, fixed #40); pcall doesn't catch a hang. Use wezterm.hostname()/static values, or a gui-startup event handler.
- [Skip redundant final review in subagent-driven-development](feedback_skip_redundant_final_review.md) — after per-task two-stage reviews pass, go straight to finishing-a-development-branch; do not dispatch a final cross-cutting reviewer. Also skip the per-task code-quality (2nd) stage for doc-only tasks (spec review suffices); keep both stages for code/logic/templates.
- [Use luaparse, not LSP, for Lua syntax checks](feedback_lua_syntax_use_luaparse_not_lsp.md) — verify wezterm.lua / Lua edits with `node /tmp/lua-syntax-check/check.js <file>` (luaparse, luaVersion 5.3), not the LSP tool; overrides the global LSP-first default. Recreate the /tmp harness if gone.
- [Check Windows chezmoi before apply](feedback_windows_chezmoi_check_before_apply.md) — when a sync must reach Windows, verify chezmoi.exe exists first; if present apply via interop, else deploy manually (never substitute the WSL Linux chezmoi — it renders templates as os=linux and re-breaks the ssh fix).
- [sudo is not passwordless here](feedback_sudo_not_passwordless.md) — Claude can't run sudo-requiring `make` targets (packages/dev/provision, `$(SUDO)`-wrapped nnd/pwndbg/vcpkg/services) non-interactively; they hang on the password prompt. Hand them to the user via the `!` prefix; lint/dry-run/`chezmoi apply`/MODE=prod still work directly. Why two CRB bugs slipped pre-merge.
- [WSL appendWindowsPath=false for fast chezmoi/czt](project_wsl_appendwindowspath_false.md) — on WSL, LookPath-heavy tools (chezmoi → czt) stall ~1.6s because ~90 /mnt/c PATH dirs get stat-walked over drvfs; fix = appendWindowsPath=false in /etc/wsl.conf (+wsl --shutdown) plus the rc re-add of just PowerShell+system32. Check this before theorizing about chezmoi/cz* slowness.
- [Verify tool bumps at runtime, not just changelogs](project_verify_tool_bumps_at_runtime.md) — sandbox-install + execute version-pin bumps on AlmaLinux 9; node 26's libatomic dep and zellij 0.44's no-web asset both passed changelog review but broke at runtime. Recipe inside.
- [winterop — WSL→Windows interop helper](project_winterop_wsl_windows_interop.md) — from a Linux-on-Windows shell use `winterop` (~/.local/bin) to reach Windows (run/path/clip/open/host/…) instead of hand-wiring powershell.exe/wslpath; detects WSL vs VM. Command map inside.
- [WSL interop binfmt dropped](project_wsl_interop_binfmt_dropped.md) — every Windows .exe failing with "Exec format error" (incl. chezmoi.exe/winterop) = the WSLInterop binfmt_misc handler is unregistered (systemd-on-WSL); re-register via sudo (hand to user, `!`) before any Windows-side work. Diagnose first: `ls /proc/sys/fs/binfmt_misc/WSLInterop`.
- [Windows apply-via-WSL gotchas](project_windows_apply_via_wsl_gotchas.md) — `chezmoi apply` hangs non-interactively on the "changed since chezmoi last wrote it" overwrite prompt for externally-modified files → use `chezmoi apply --force` (diagnose with read-only `cat`/`diff`); Stop-Job orphans the chezmoi child holding the lock; never run bootstrap.ps1 concurrently; targeted apply won't mkdir a missing parent. WSL-spawned powershell shares the user's SessionId 1.
- [Windows Nerd Font name + activation](project_windows_nerdfont_activation.md) — WT/DirectWrite needs the Win32 family name `JetBrainsMono NFM` (not the long typographic name); per-user fonts aren't live until logon or AddFontResourceW+WM_FONTCHANGE (bootstrap step 7); WezTerm masks a missing/misnamed font via font_dirs+fallback, so it's not proof the system has it.
